# MoonBit build artifacts
target/
_build/
.repos/
.mooncakes/
*.core
moon_workspace_lock*

# OS / editor
.DS_Store
*.swp

# Local agent/client config (contains secrets, never publish)
.zcode/

# Session-orchestration files live outside the repo (see ../AGENTS.md)
AGENTS.md
