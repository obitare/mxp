name = "obitare/mxp-core"

version = "0.1.0"

readme = "README.mbt.md"

repository = ""

license = "Apache-2.0"

keywords = [ "moonbit", "vscode", "chrome", "extension", "manifest" ]

description = "Host-agnostic core logic for the mxp extension toolkit"
